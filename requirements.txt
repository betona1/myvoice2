# FastAPI 서버
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.9

# TTS 엔진 (coqui-tts 포크, 기존 TTS 패키지는 PyPI에서 제거됨)
coqui-tts>=0.27.0
transformers<5

# PyTorch (GPU 버전 - CUDA 12.8 기준)
# CUDA 버전에 따라 아래 명령어로 별도 설치:
# pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128

# 오디오 처리
soundfile==0.12.1
librosa>=0.11.0
pydub==0.25.1

# DB
aiosqlite==0.20.0

# 화자 분리
speechbrain>=1.0.0

# 유틸리티
python-dotenv==1.0.0
aiofiles==23.2.1

# 인증
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
aiosmtplib==3.0.2
httpx>=0.27.0

# 중국어 발음 학습
pypinyin>=0.55.0
praat-parselmouth>=0.4.7
faster-whisper>=1.2.0
opencc-python-reimplemented>=0.1.7
pdfplumber>=0.11.0
pdf2image>=1.17.0

# 일본어 학습
pykakasi>=2.2.0
jaconv>=0.5.0
cutlet>=0.4.0
fugashi>=1.3.0
unidic-lite>=1.0.8
Levenshtein>=0.21.0
pandas

# 2026-08-28 누락 복구 — 이미지 빌드 시 빠져 있어 런타임에 조용히 실패하던 것들
translators           # /api/chinese/translate 한국어 번역 (없으면 해석이 빈칸으로 나옴)
gspread               # /api/sheets/* 구글시트 교안 연동
google-auth-oauthlib  # 구글시트 OAuth 플로우
pytesseract        # 단어앱 카메라 검색 OCR (tesseract 바인딩)
jieba              # 촬영 검색·문제 지문의 한자 끊기 (/api/words/ocr, 문제풀이 낱말 나누기)
